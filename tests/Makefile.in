LIBS = thread_pool
TESTS = stm mutex
CLEAN_ALL = $(addsuffix .clean, $(LIBS) $(TESTS))

.PHONY: $(LIBS) $(TESTS)

all: $(LIBS) $(TESTS)

$(LIBS):
		$(MAKE) -C $@ all

$(TESTS):
		$(MAKE) -C $@ all

clean: $(CLEAN_ALL)
$(CLEAN_ALL):
		$(MAKE) -C $(basename $@) clean
